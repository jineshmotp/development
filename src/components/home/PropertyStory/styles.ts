import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  titleContainer: {
    position: 'absolute',
    bottom: 100,
  },
  subContainer: {
    height: 180,
    width: 140,
    borderRadius: 8,
    position: 'relative',
    flex: 1,
  },
});
