import { StyleSheet } from 'react-native';

import { px } from '@/utils';

export const styles = StyleSheet.create({
  sheetContent: {
    flex: 1,
    backgroundColor: 'white',
    borderTopLeftRadius: 20,
    borderTopRightRadius: 20,
    marginBottom: px(10),
  },
  main: {
    backgroundColor: '#5151518c',
  },
});
