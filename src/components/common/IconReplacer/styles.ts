import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  main: { width: 55, height: 55, alignItems: 'center', justifyContent: 'center', marginRight: 10 },
  iconCircle: {
    width: 55,
    height: 55,
    borderRadius: 55,
    backgroundColor: 'white',
    alignItems: 'center',
    justifyContent: 'center',
    marginRight: 10,
  },
});
